{
  "name": "firebase",
  "version": "1.0.0",
  "description": "Firebase local docker server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-server": "^0.12.0"
  }
}
